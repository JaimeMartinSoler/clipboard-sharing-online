{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "migrations:apply": "wrangler d1 migrations apply clipboard-sharing-online"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.17.0",
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.7.0",
    "vite": "^7.3.6",
    "vitest": "4.1.9",
    "wrangler": "^4.106.0"
  }
}
